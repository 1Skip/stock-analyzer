# 2026-07-15 短线板块边界恢复

## 已完成

- 按用户确认，普通`短线`只保留`全部`板块入口；普通短线原有候选池、技术过滤、热门板块辅助评分、上下文评分、排序和学习层不改。
- `短线经典版`恢复最初板块入口：`全部、苹果概念、特斯拉概念、电力、算力租赁`。
- 经典版专题板块使用真实同花顺/东方财富板块成分获取，失败时回退到项目原始沪深主板兜底池；板块内只执行成交量、MACD、RSI、KDJ、BOLL 技术过滤，不启用热门板块、基本面、财报、资金流、消息面或美股联动辅助。
- T+1 预热同步为普通短线只生成`全部`，经典版生成`全部`加配置的四个专题板块，其他策略仍只生成`全部`。
- 修正算力租赁兜底池 `000938` 的名称为`紫光股份`，与项目全量静态股票名称索引一致。

## 关键真实板块映射

- 苹果概念：同花顺概念代码 `300309`
- 特斯拉概念：同花顺概念代码 `301121`
- 电力：同花顺行业代码 `881145`
- 算力租赁：同花顺概念代码 `309068`

## 验证

- 针对性测试覆盖普通短线非`全部`路由拒绝、经典版专题路由、五项 UI 板块选项、经典版纯技术 `include_context=False` 和 T+1 目标展开。
- 真实接口检查中，苹果概念、特斯拉概念、电力、算力租赁分别从同花顺板块详情返回 10、10、20、10 只成分股，四组状态均为 `fresh`。
- Ruff、文档编码、离线数据契约、Git 跟踪 Python 文件编译检查通过；完整测试 `988 passed, 20 warnings`。
