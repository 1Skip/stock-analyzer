# 2026-07-16 UI 与 T+1 回看修复

## 已完成

- 修复 A 股大盘温度显示：上涨为红色向上箭头，下跌为绿色向下箭头，平盘为灰色且不显示箭头。
- 修复回测页“超额收益”显示：正收益为红色向上箭头，负收益为绿色向下箭头，零值为灰色且不显示箭头。
- 修复日报在 `include_recommendations=False` 时把 T+1 历史回看一并清空的问题。推荐池扫描与已保存历史读取现在独立，T+1 读取失败会记录异常日志。
- 涉及 `run_t1_plan` 的单元测试改用 `FakeCache()` 作为历史缓存，避免测试记录写入本地真实 T+1 历史。
- 清理本地 T+1 历史缓存中的 449 条明确测试记录，保留 206 条真实计划；缓存及备份均被 `.gitignore` 忽略，不提交到 Git。

## 已验证

- `tests/test_daily_report.py tests/test_recommendation_service.py`：`48 passed`。
- `tests/test_ui_enhancements.py`：`110 passed`。
- Ruff、Python 编译检查、`git diff --check` 通过。
- 清理缓存后重跑相关测试，真实 T+1 缓存 SHA256 前后保持一致。
- 浏览器实页确认历史日报显示最近 10 份计划、49 只标的、39 只已完成、1 日平均收益 `+0.37%`、1 日胜率 `51.28%`，并确认回测正超额收益显示红色向上箭头。

## 口径边界

- 历史日报中的 1 日胜率是已完成 T+1 样本的回看统计，用于观察推荐结果表现，不等同于模型的长期准确率或未来收益保证。
